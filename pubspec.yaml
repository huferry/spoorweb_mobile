name: spoorweb_mobile
description: Spoorweb Mobile
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  google_maps_flutter: ^2.0.3
  firebase_core: ^1.6.0
  cloud_firestore: ^2.5.1
  intl: ^0.17.0
  firebase_messaging: ^11.2.0
  
dev_dependencies:
  flutter_test:
    sdk: flutter


flutter:
  uses-material-design: true
  assets:
    - assets/logo.png
